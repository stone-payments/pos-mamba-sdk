import defaultKeyboard from '../keyboards/default';
import type { KeyboardTypesPredefinedOptions } from '../types';

/**
 * Get default keybaord
 * @returns Default alphabet layout
 */
export function getDefaultKeyboard(): KeyboardTypesPredefinedOptions {
  return defaultKeyboard;
}
