import { MODELS } from '@mamba/utils';

const KEY_ACTION = {
  UP: 'up',
  DOWN: 'down',
};

const ARROW_CAPABILITIES_MODELS = [MODELS.MP35P, MODELS.Q60, MODELS.D195];

export { KEY_ACTION, ARROW_CAPABILITIES_MODELS };
