/**
 * Active organization file on POS root filesystem « `activeOrg.json`
 *
 * @example
 * {
 *   "active_org": "stone",
 *   "org_package_folder": "/data/app/MAINAPP/orgs/stone"
 * }
 */
const activeOrgFileName = 'activeOrg.json';

const activeOrgFiled = 'active_org';
const packageFolderField = 'org_package_folder';

const organizationFileName = 'appOrgParams.json';

export { activeOrgFileName, activeOrgFiled, packageFolderField, organizationFileName };
